import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Modern Calculator - Glassmorphism Design",
  description:
    "A beautiful, modern calculator with glassmorphism design. Features basic arithmetic, advanced mathematical functions, and calculation history.",
  keywords: "calculator, math, glassmorphism, modern design, web app",
  authors: [{ name: "Calculator App" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
